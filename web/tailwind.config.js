// HeroUI v3 no longer requires Tailwind plugin integration.
// Theme configuration is now handled via @heroui/styles in globals.css.
// Custom theme extensions are declared with @theme in globals.css.

/** @type {import('tailwindcss').Config} */
export default {
  darkMode: "class",
};
